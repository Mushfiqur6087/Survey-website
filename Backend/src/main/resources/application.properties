#### application properties for mushfiqur
####------------------------------------

# Server configuration

server.port=8080

# PostgreSQL Database configuration
spring.datasource.url=jdbc:postgresql://localhost:5432/surveydb
spring.datasource.driverClassName=org.postgresql.Driver
spring.datasource.username=postgres
spring.datasource.password=postgres
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# JPA configuration
spring.jpa.hibernate.ddl-auto=update
# change to 'update' for production
spring.jpa.show-sql=true
spring.jpa.format-sql=true

# Application configuration
spring.application.name=survey-backend

# Admin configuration
admin.default.username=a7f32c9b4e218f6d
admin.default.password=c9e7b2d19f0a36a4 

# Submission password configuration
submission.password=4be91df83c7e21b0

#### application properties for mushfiqur
####------------------------------------